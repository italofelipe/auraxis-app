export const AI_INSIGHTS_CONTEXTUAL_FEATURE_FLAG_KEY = "app.insights.contextual";

/**
 * Feature flag that gates the new "Fluida" editorial insights screen.
 * Default OFF (catalog status `draft`) so the legacy hub keeps serving
 * production while the new reading is built incrementally.
 */
export const AI_INSIGHTS_FLUIDA_FEATURE_FLAG_KEY = "app.insights.fluida";
