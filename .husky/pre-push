#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Type-check completo antes do push
echo "🔍 Running TypeScript check before push..."
npx tsc --noEmit

# Testes com coverage
echo "🧪 Running tests with coverage..."
npx jest --passWithNoTests --coverage

echo "✅ Pre-push checks passed."
